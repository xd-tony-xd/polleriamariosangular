
export const environment = {
  // 'production' debe ser 'true' para que Angular aplique optimizaciones de rendimiento,
  // deshabilite ciertas comprobaciones de desarrollo y use el modo de producción.
  production: true,

  // URL base de tu API para el entorno de producción (despliegue real).
  // Reemplaza 'https://api.tuservidor.com' con la URL real de tu backend en producción.
  apiUrl: 'https://api.tuservidor.com',

  // Puedes añadir otras variables específicas de producción aquí, como claves públicas de
  // servicios externos, flags de características, etc. (¡Nunca secretos sensibles!).
  // por ejemplo:
  // featureToggle: false,
};