// src/environments/environment.development.ts
export const environment = {
  production: false, // Indica que no estamos en producción
  apiBaseUrl: 'http://localhost:3000/api', // URL local de tu API para desarrollo
  // Otras variables específicas de desarrollo (ej. claves de prueba)
};